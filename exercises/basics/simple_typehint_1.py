
# We can define the expected type of a variable
# by writing ' : type' after the variable name
# example:
# we define that x and y shall be integers
# this is possible with every type
def do_add(x: int, y: int):
    result = x + y
    print(result)

# TODO: add reasonable typehints to that function
def add_strings(str_1, str_2):
    result = str_1 + str_2
    print(result)

# TODO: write a function that takes three ints, adds them together and prints the result
#  add typehints to the parameters
